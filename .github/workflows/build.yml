name: Build Windows EXE

on:
  workflow_dispatch:  # 允许手动触发
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest  # 在真实的 Windows x64 环境运行！

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install pyinstaller

    - name: Build EXE
      run: |
        pyinstaller --onefile --windowed --name="XPT转CSV转换器" xpt_to_csv_converter.py

    - name: Upload EXE as artifact
      uses: actions/upload-artifact@v3
      with:
        name: XPT-Converter-Windows
        path: dist/